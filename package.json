{
  "name": "dashbordsupermarket",
  "private": true,
  "description": "Monorepo root. Next.js app is in /platform. See DEPLOY_VERCEL.md and root vercel.json for Vercel.",
  "scripts": {
    "build": "npm run build --prefix platform",
    "dev": "npm run dev --prefix platform",
    "start": "npm run start --prefix platform"
  }
}
